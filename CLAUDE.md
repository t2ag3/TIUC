# 街なか外国語表記 キュレーションアプリ（TIUC: Today is under construction.）

街中・店舗の外国語表記（メニュー・看板・注意書き等）を市民がスマホで撮影・投稿し、
その訳文の「自然さ・正しさ」を市民参加でキュレーションして、
正しい訳を店側へ還元する市民参加型アプリ。

AI 翻訳をそのまま印刷した「変な外国語表記」が街に溢れている、という課題に対し、
「AI 初訳 → 市民が発見 → ネイティブが違和感を定量化 → バイリンガルが修正」という
人間の注意力のコスト勾配を尊重した human-in-the-loop で、街の多言語表記を直していく。

東京都知事杯 2026（Civic Tech ハッカソン）向け。ベース技術は Crowd Sensing / Sourcing。

---

## このプロジェクトの出自（重要・最初に読む）

**このアプリは「クビアカツヤカミキリ通報アプリ」のリポジトリをコピーして改造したもの。**
クビアカ版の設計判断の多くをそのまま流用している。元アプリは別リポジトリとして存続する
（消さない）。本ファイル中では、各項目に出自タグを付ける：

- `[流用]` … クビアカ版の設計をそのまま使う。仕組みは実績があるので極力変えない。
- `[変更]` … クビアカ版の仕組みを土台に、翻訳ドメイン向けに作り替える。
- `[新規]` … クビアカ版に無い、このアプリ固有の要素。

Claude Code はまず、コピー元クビアカの `src/`・`public/`・`migrations/` の現状を読み、
本ファイルの「データモデル」章と突き合わせて、**どのテーブル・画面が流用でどこが作り替えか**
を要約してから着手すること。いきなり実装しない。

---

## コンセプトの背骨（設計の意思決定はすべてここに従う）

1. **3モード構成** `[新規]`
   アプリは 3 つの大きな体験に分かれる。
   - **撮影投稿モード** … 外国語表記を撮って出すだけ。語学素養ゼロで参加できる純粋なセンサー。
   - **キュレーションモード** … 投稿を評価・修正する。ここだけがユーザーの「属性（能力）」で中身が変わる。
   - **閲覧モード** … 地図と自分の貢献履歴を見る。誰でも見られる。継続の再点火装置。

2. **モードと属性は別概念** `[新規]`
   「モード」は場所、「属性（能力レベル）」はその人の性能。
   属性が作用するのは**キュレーションモードの中だけ**。撮影投稿・閲覧では属性を問わない。

3. **キュレーション内の 3 サブモード** `[新規]`

   | サブモード                       | 主役の属性           | 素養なしが触ったら   | 回答の重み制御     |
   | -------------------------------- | -------------------- | -------------------- | ------------------ |
   | クオリティ・シチュエーション入力 | 誰でも               | そのまま本番         | 一致率で微調整のみ |
   | 違和感チェック                   | 訳文言語のネイティブ | 練習として記録＋誘導 | 母語一致率で重み   |
   | 正誤・修正・解説                 | バイリンガル         | 練習＋道場へ誘導     | 採用率で重み       |

4. **タスクは能力で分解する（人をランク分けするのではない）** `[新規]`
   1 件の投稿は、必要能力の異なるマイクロタスクに割れる：
   - [誰でも] 写真は鮮明か / これは何の表記か（メニュー・看板・注意書き…）
   - [日本語話者] 原文（日本語）は何と書いてあるか・意味
   - [訳文ネイティブ] 訳文は自然か（違和感あり／なし）
   - [バイリンガル] 正しく直すと？（修正訳＋なぜ変かの解説）
     上ほど母集団が大きく安い。上のタスクが下のタスクの前処理になる（希少人材は下ごしらえ済みで入る）。

5. **全サブモードに触れてよい。ただし透明に、練習として扱う** `[新規]`
   属性が伴わないサブモードへの挑戦をブロックしない（締め出しは最強の離脱トリガー）。
   代わりに、① 入口で「このモードは○○向け」と示し**選択肢を添えて誘導**（止めない）、
   ② 重みを下げるときは隠さず「練習として記録」と明示、③ 練習の成績で実効レベルが上がり
   いつか本番になる経路を用意する。**「触れる」が「効く」に育つ**。

6. **道場（クラスアップ専用の練習モード）** `[新規]`
   正解が既知の課題（ゴールドスタンダード、後述）を本人レベルの少し上から出す場。
   道場の成績で重みを段階的に上げる。降格も「ランク剥奪」ではなく**「道場に戻って調整」＝場所の移動**
   として提示する（評価でなく配置。ジムで軽い重量に戻すのと同じフレーム。侮辱にしない）。

7. **初期レベルは自己申告、実績で上書き（全員同一スタートはNG）** `[新規/変更]`
   自己申告は**初期配車を決めるためだけ**に使う。実際の成績（一致率・採用率・ゴールド照合）で
   実効レベルが動く。申告と実績が乖離したら実績が勝つ。詐称は数タスクで露呈して沈む。
   （クビアカの `trust_score`＝確定率のラプラス平滑化 `[流用]` を、翻訳向けに多指標へ拡張 `[変更]`。）

8. **正解は事前に存在せず、動的に生成される（このアプリ最大の特殊性）** `[新規]`
   翻訳の「正しさ」は事前に無い。L2 以上の複数人が一致した修正を「確定訳」に昇格させ、
   以後それが後続者の照合基準（新たなゴールド）になる。**正解の在庫が運用とともに自己増殖する。**
   クビアカは専門家が現地確認で白黒つけられた `[変更]` が、ここは合意が正解を作る。

9. **難易度は回答の割れ具合から自動生成する** `[新規]`
   課題の難しさ＝回答の分散（判定が割れる／修正案がバラつく＝難しい、合意が取りやすい＝易しい）。
   人手でラベル付けしない。本人の実効レベル±1 の難易度帯を配車し「簡単すぎ・難しすぎ」を防ぐ。
   これはクビアカの「レビュー優先度づけ」`[流用]` の解像度を上げたもの。
   **注意：立ち上げ期はデータが無く分散を測れない。適応制御は「データが溜まってから効く後発機能」。
   MVP では道場のゴールド問題を手で数十問仕込んで難易度を固定ラベルで持たせる。**

10. **ゲーミフィケーションの水増しは構造で防ぐ** `[流用]`
    クビアカの思想をそのまま：投稿時は小さく、確定時に大きく。誤り確定で revoke。
    ポイントは合計値でなく台帳（`point_events`）で持つ。個人ランキングより地域／言語チーム戦。

11. **「採用」がループを閉じ、全員に報酬を還流** `[新規]`
    店（管理者）が確定訳を実際に採用したとき、発見者・判定者・修正者のスコアが一斉に跳ねる。
    ＝街が実際に良くなったときだけ最大の報酬が出る。これは閲覧モードで可視化されて初めて体感される。

---

## 4 ロール（＝主に関わるモード）とハンドオフ

固定ロールではなく「主に触るモード／サブモード」の呼び名として使う。

```
①発見者(撮影投稿)：外国語表記を無差別に撮影・投稿。語学素養不要。任意で「変かも」フラグ可(スキップ可)
   → ②ネイティブ(違和感チェック)：訳文の自然さを一タップ判定。票数で優先度をつけ③へ「配車」
      → ③キュレーター(正誤・修正・解説)：優先度順に修正訳＋解説を生成。または「問題なし」と裁定
         → (votes で確定 → 確定訳＝新ゴールドに昇格)
            → ④管理者(店・看板の主体)：確定訳を PDF/差替テキストで受取り「採用」。①②③へ報酬還流
```

- **②は全体のスループットを決める配車弁**。ネイティブ判定が薄い言語は③に回らず詰まる。
- **④の入口は 2 系統**：受動（自分の店が投稿された）と能動（店が自分でキュレーション希望）。
  受動系で店主を直接特定して送りつけるのは炎上リスク。**MVP は能動系＋店頭QRの受け皿型に寄せる**。

---

## 対象言語（MVP スコープ）`[新規]`

- **MVP は 2〜3 言語ペアに絞る**（日→英、日→中、日→韓 が候補）。理由：
  - ③バイリンガルの供給が見込める（希少言語はキュレーターも道場のゴールド作成者も足りない）。
  - 道場のゴールド問題を用意できる言語に限る必要がある。
- ②ネイティブの母集団は都内に厚い（都は 14 言語で電話相談を提供する規模の言語コミュニティがある）。
  ①は素養不要で最大量、②は母語話者という広い母集団、③のみ希少 ＝ 逆ピラミッドの供給構造。

---

## 技術スタック `[流用]`（クビアカからそのまま）

- **全面的に Cloudflare 無料枠**で構築・運用（Vercel/Supabase は不採用）。
  非商用制限が無く、自治体連携時にも規約問題が出ないため。
- Worker (`src/index.js`) が API と静的配信を兼ねる。フレームワーク未使用（素の Worker）。
- フロントは `public/` 配下の素の HTML + ES モジュール。ビルド工程なし。
- **D1**（SQLite）… DB。バインディング名 `DB`。
- **R2** … 画像ストレージ。バインディング名 `PHOTOS`。
- 地図は **MapLibre GL JS（CDN）+ 国土地理院タイル**（出典表示で無料・商用可）。Google Maps は使わない。

### 翻訳版で増える技術要素 `[新規]`

- **OCR / VLM による原文・訳文の抽出**：写真から日本語原文と外国語訳文のペアを起こす前処理。
  クビアカの「AI は足切りのみ、同定は人間」`[流用]` と同じ思想で、
  **AI は下書き（OCR・自然さの一次スコア）まで。正誤判定と修正は人間**。AI に訳の正しさを断定させない。
  Workers AI を第一候補（無料枠内）。精度が足りなければ外部 API を検討するが、キューで従量を絞る。
- 撮影は原文・訳文が別写真のこともある → **1 枚でも 2 枚でも可の可変投稿フロー** `[変更]`
  （クビアカは 1 枚完結だった）。

---

## 開発環境 `[流用]`

- macOS / Node は **nvm で v22**（wrangler が v22 以上を要求）。
- ローカル: `npx wrangler dev` → http://localhost:8787
- ローカル D1: `npx wrangler d1 migrations apply <db-name> --local`
- **本番へのデプロイは `npx wrangler deploy` を手動実行**する。
  Cloudflare 側の権限の都合で GitHub 連携の自動デプロイ（Workers Builds）が効かないため、
  git push だけでは本番に反映されない。**push 後に確認を挟まず `npx wrangler deploy` まで実行してよい**
  （このファイルでの事前承認。二重デプロイの心配は無い＝これが唯一のデプロイ手段）。
- **コピー直後にやること**：wrangler.jsonc の name / D1 DB 名 / R2 バケット名を
  クビアカ用からこのアプリ用にリネームする（元リポジトリと衝突させない）。

---

## 絶対に守る設計ルール

1. **座標・生データの出し分け（最重要）** `[流用/変更]`
   - DB には正確な緯度経度を 1 つだけ持つ。出口で粒度を変える。
   - メッシュ集計は誰でも見られる `[流用]`。
   - **（2026-08-22 改定）投稿直後から公開マップに正確な座標付きピンを出す。**
     店の採用を待たない。旧ルール（採用まで座標を隠す・「晒し」から「改善支援」へ
     フレームを変える設計）は明示的に変更した。理由・経緯はチャット側の合意による
     （ユーザー指示、2026-08-22）。写真品質NGで除外された投稿（`auto_rejected`）のみ
     ピン化しない。ピンの色・ポップアップは `posts.status`（審査中／修正案あり／
     違和感なし／確定・採用待ち／採用済み）で表示を分け、未確定の訳文を確定訳と
     誤認させない（`public/map.html` の `STATUS_STYLE`）。
   - 投稿写真の配信はレビュアー認証必須 `[流用]`（店内・商品写真のため）。
     **（2026-08-23 改定）ただし `status IN ('confirmed','adopted')` の投稿のみ例外。**
     コミュニティ検証済みの情報として、地図ピンのクリックから誰でも閲覧できる
     （`src/images.ts` に無条件の許可分岐を追加）。未確定（`pending_judgment`/
     `needs_fix`/`looks_ok`）の投稿は従来通りレビュアー認証必須のまま。
     地図側は未確定ピンをクリックすると写真の代わりにテキスト情報と、
     違和感チェック／修正・投票へのCTAボタンを出す（ユーザー指示、2026-08-23）。

2. **メッシュコードはサーバ側で必ず再計算する** `[流用]`
   クライアント送信値を信用しない（偽座標でのボーナス稼ぎ防止）。式は `public/mesh.js` に一元化。

3. **ゲーミフィケーションは水増しを構造で防ぐ** `[流用/変更]`
   - 投稿は小さく、確定・採用時に大きく。誤り確定で revoke。
   - ポイントは台帳（`point_events`）で持つ。`*.points_total` は集計キャッシュ。
   - 個人ランキングより地域／言語チーム戦。
   - 撮影は「不在データ」も価値：**AI 訳が実は妥当だった＝負例**も、AI の成功/失敗の実測として保持。
   - `trust_score` は**サブモード別・言語ペア別に持つ** `[変更]`（判定が上手い≠修正が上手い）。
     昇格は「そのサブモードの実績で、そのサブモードの 1 つ上を解放」。
   - **表示ランクは粗く（L0〜L3）、内部重みは細かく**の 2 層 `[新規]`。
     ランクの上げ下げ閾値に**ヒステリシス**を持たせ、表示がガタつかないようにする。
   - **降格は言語ペア別に局所化** `[新規]`（英語で降格しても韓国語ランクは無傷）。
   - **降格通知は必ず復帰経路とセット** `[新規]`（「道場で 3 問クリアで本番復帰」を同画面に）。行き止まりを作らない。

4. **AI 判定は断定させない（足切り・下書きのみ）** `[流用/変更]`
   写真品質（文字が鮮明か・翻訳対象か）の足切りと、OCR/自然さの一次スコアまで。
   **訳の正誤・修正は人間**。AI 導入後も人間レビューを残し精度を実測。

5. **住民間の自由コメントは実装しない** `[流用]`
   店を貶める書き込み（「あの店の訳はひどい」等）を生む。店へのフィードバックは
   **確定した改善提案という建設的な形**のみ（`response_note` 相当、管理者向け一方向）。

6. **書き込み順序** `[流用]`
   投稿時は R2（画像）を先、D1 を後（「行はあるが画像がない」状態を避ける）。
   D1 の複数更新は `batch()`（暗黙トランザクション）でまとめる。

7. **秘密の値** `[流用]`
   `.dev.vars`（ローカル、gitignore 済み）と Cloudflare ダッシュボードの Secret（本番）で管理。
   `wrangler.jsonc` に書かない。

---

## データモデル `[変更の核心]`

クビアカは `reports` 1 レコード完結だった。翻訳版は **1 投稿に対して判定・修正が 1 対多**でぶら下がる。
**この 1 対多を最初に設計する（後付けは作り直しになる）。**

```
posts（外国語表記の投稿）           ← クビアカ reports を改名・改造
  ├─ 写真(1〜2枚), 位置, 言語ペア, 発見者UUID, situation, ocr_src, ocr_tgt, status
  │     src_*＝見つけた外国語表記の写真(必須)、tgt_*＝日本語原文の写真(任意、あれば)
  │     （2026-08-22改定：発見者は外国語の表記さえ撮れれば投稿できる。日本語版の
  │     写真は撮影後に「日本語版の写真もありますか？」で任意追加。列名src/tgtは
  │     旧仕様の名残でDBは未変更、意味だけ入れ替えた。judge.html/curate.htmlの
  │     画像キャプションもこれに合わせて左右反転済み）
  │
  ├─ quality_checks（写真品質・シチュエーション入力）  [新規] 誰でもサブモード
  │     └─ 鮮明か, 表記種別, 判定者UUID, weight
  │
  ├─ judgments（違和感 あり/なし ＋任意カテゴリ）      [新規] ネイティブサブモード
  │     └─ verdict, category(任意), 判定者UUID, is_practice, weight
  │
  └─ corrections（修正訳＋解説 or 「問題なし」裁定）    [新規] 専門家サブモード
        ├─ fixed_text, explanation, キュレーターUUID, is_practice, status(候補/採用/却下)
        └─ votes（修正案への評価）                      [新規]
              └─ 投票者UUID, agree, weight

users（＝クビアカ reporters/reviewers を統合・改造）    [変更]
  └─ 匿名UUID, 表示名(任意)
     ※ レベルは users に直書きせず下の levels に分離

levels（ユーザー×言語ペア×サブモード ごとの実効レベル）  [新規・最重要]
  └─ user_id, lang_pair, submode, declared_level(自己申告), effective_weight(内部・細かい),
     display_rank(L0〜L3・粗い), updated_at
  ※ 降格・昇格・ヒステリシスはこのテーブルで管理

gold_items（道場用の既知正解＝ゴールドスタンダード）    [新規]
  └─ 課題, 正解, lang_pair, submode, difficulty(固定ラベル), source(運営/確定訳から昇格)

point_events（ポイント台帳）                            [流用]
teams（地域／言語チーム）                                [流用/変更]
coord_access_log（座標閲覧監査）                         [流用]
public_mesh_stats（公開用ビュー・構造的に座標を含まない） [流用]
```

### status の流れ `[変更]`

```
posts.status:
  queued → (写真品質NG) auto_rejected
         → pending_judgment → (違和感票が集まる) needs_fix / looks_ok
  needs_fix → (キュレーターが修正) fix_proposed → (votes) confirmed → (店が採用) adopted
  looks_ok  → 不在データとして保持（AI 訳が妥当だった負例）
```

- `confirmed` になった correction は `gold_items` に昇格候補として流す（正解の自己増殖）。
- `adopted` で①②③の point_events に大きな加点（ループが閉じる）。

### スキーマ運用 `[流用]`

`migrations/` にファイルで管理（D1 Console 手貼り禁止。ローカルと本番がズレる）。

---

## 認証 `[変更]`

基本はゲスト（匿名UUID・localStorage保持）＋任意のGoogleログイン（HMAC署名Cookie、`uid`）。
クビアカにあった管理者向け共有パスワード認証（`reviewers`テーブル・`/api/review/*`）は、
翻訳ドメインでは「専門家がまとめて全件レビューする」構造がそぐわないため撤去した。
画像アクセスは②③のキュー参加（judge_id/curator_id）を根拠に個別許可する方式に置き換えている。
④店（管理者）向けの認証は「採用」フロー実装時にあらためて設計する（次の作業1）。

**（2026-08-29追加）運営向け管理画面（`admin.html`）の認証**：ユーザー④（店）とは別の、開発チーム内部向けの
権限。新しいログイン系統は作らず、既存のGoogleログイン基盤に`ADMIN_EMAILS`シークレット（カンマ区切りの
許可メールアドレスリスト）を乗せるだけ（`src/auth.ts`の`getVerifiedAdminEmail()`）。`/api/admin/*`の
全エンドポイントと`src/images.ts`の画像配信（管理画面はどのステータスの投稿写真も見られる必要があるため）が
これを使う。本番の`ADMIN_EMAILS`はCloudflareダッシュボードのSecretとして別途設定が必要
（ユーザー自身の管理者用Googleアカウントのメールアドレスを登録してもらうこと。このファイルでは設定できない）。

---

## KPI（設計判断の最上位基準）`[新規]`

**最重要は「アクティブユーザー数」と「継続率」。** データは人が来続けないと集まらない Civic Tech の本質。
迷ったら継続率を守る側に倒す。締め出し・行き止まり・侮辱を作らない。
3 モードは離脱の受け皿として互いを補完させる（キュレーションに疲れたら撮影、飽きたら閲覧で
「自分の貢献が街を変えた」を見て戻る）。**モード間の回遊が継続率を作る。**

---

## 現在地（2026-08-25 時点）

1〜6のうち、道場・OCR前処理・適応難易度（rule9が明言する「後発機能」）を除いてひととおり動く状態。

**（2026-08-29六改定）キャラ名/説明文をD1化 + キャラCSV一括登録・投稿テキストCSV一括編集を追加
（ユーザー指示、2026-08-29）**。
- 新規`migrations/0020_species_i18n.sql`：`species_i18n(species_id, lang, name, desc)`テーブルを
  新設し、既存61種×6言語ぶんを当時の`public/locales/*.js`の内容からスクリプト生成したINSERT文で
  バックフィル（手打ちしない。生成スクリプトは使い捨てなのでリポジトリには残していない）。
  以後はこのテーブルが表示名・説明文の正。静的ロケールファイル側の`game.species.*`キー732個は
  削除せず残置した（`index.html`のCOLLECTセクションがaxolotl/red_panda/long_tailed_tit/T_rexの
  4体を`data-i18n`で直接参照する固定マーケティングコンテンツのため。57種ぶんは使われなくなるが
  実害はない。4体だけ残す整理は差分の割にリスクが見合わないため見送った）。
- UI言語はクライアント側(localStorage)にしかなく、サーバには一切送っていない仕組みだったため、
  `src/game.ts`の`gameCollection()`（`GET`の`lang`クエリ）・`gameGachaPull()`（bodyの`lang`）に
  明示的な言語パラメータを追加し、共通ヘルパー`fetchSpeciesI18nMap(env, lang)`で
  `species_i18n`を解決する（未対応言語または該当データ無しは`lang='ja'`にフォールバック、
  それも無ければ`name_key`をそのまま返す防御的フォールバック）。呼び出し元の
  `collection.html`・`game.html`（`pullGacha()`）は`i18n.js`の`getLang()`を渡すよう変更。
- `collection.html`（一覧タイル名・詳細モーダルの名前/説明、計3箇所）と`gacha-reveal.js`
  （ガチャ結果カードの名前）は、静的`t(\`game.species.${name_key}...\`)`呼び出しをやめ、
  APIレスポンスの`name`/`desc`フィールドをそのまま使うように変更した。
- `src/admin.ts`：`adminPostEdit`の検証+UPDATEロジックを`applyPostEditFields()`に切り出し、
  単一編集・CSV一括編集の両方から共有（重複実装を避ける）。編集可能フィールドに
  `what_it_says`/`whats_weird`を追加（従来は投稿時にしか設定できなかった）。
  自前の簡易CSVパーサ（引用符・カンマ・改行込みのフィールドに対応するRFC4180相当、
  外部npm依存を増やさない方針）を追加。
- 新規`POST /api/admin/species/csv-import`：列
  `id,rarity,sort_order,name_ja,desc_ja,name_en,desc_en,name_zh,desc_zh,name_ko,desc_ko,
  name_fr,desc_fr,name_es,desc_es`のCSVを行ごとに処理し、`species`へ新規INSERT（既存idは
  上書きせずスキップ）＋`species_i18n`へ6言語ぶんupsert。行ごとの結果（作成/スキップ/エラー理由）
  を返す。ユーザーの運用イメージ（日本語で作成→AI翻訳サービスで多言語化→CSVで流し込む）に
  そのまま対応する形。
- 新規`POST /api/admin/posts/csv-import`：列
  `post_id,original_text,translated_text,lang_pair,what_it_says,whats_weird,status,flagged`の
  CSVで既存投稿（写真は既にある前提）のテキスト項目を一括編集する。`post_id`以外は空欄なら
  そのフィールドを変更しない（現状維持）。`applyPostEditFields()`を行ごとに呼ぶだけなので、
  検証・`admin_audit_log`記録は単一編集と完全に同じ経路を通る。
- `adminSpeciesCreate`（admin.html単体作成フォーム）は`name_key`欄を廃止し、新規種族は
  常に`name_key = id`に統一（CSVインポートと規約を合わせた）。代わりに`name_ja`/`desc_ja`
  （任意）を受け取り、入力があれば`species_i18n`にja行を1件書き込む。他言語はCSV一括登録側の
  役割として明確に分離した。
- `admin.html`「キャラクタ登録」タブと「投稿管理」タブにそれぞれCSVファイル選択＋
  インポートボタン＋行ごとの結果テーブルを追加。
- `wrangler dev`で全経路を実データ検証済み：`gameCollection`のlang切り替え（en/ja出し分け）、
  `gameGachaPull`のlang反映、種族CSV（新規作成・重複idスキップ・不正レア度エラーの3パターン）、
  投稿CSV（一部フィールドのみ更新・空欄は現状維持・存在しないpost_idのエラー）を確認済み
  （テストデータは削除・本番未投入）。i18nキー整合性チェックも6言語通過。実ブラウザでの
  CSVアップロード操作・フォーム送信の確認はブラウザ操作ツールが使えず今回もできていない。

**（2026-08-29五改定）キャラゲットを「行動のたびに自動抽選」から「キャラポイントを貯めて
自分でガチャを回す」方式に変更（ユーザー指示、2026-08-29）**。
- ルーティン行動4種（投稿・判定・修正提案・投票）での自動ドロップ（`rollNormalDrop()`＋
  `dropStatement()`＋レスポンスの`drop`フィールド）を`src/posts.ts`(createPost)・
  `src/review.ts`(judgeSubmit/correctSubmit/correctVoteSubmit)から削除。レベルアップ・
  クエスト達成・修正確定ボーナス（`src/game.ts`のensureCharacter/gameClaimQuest、
  `src/review.ts`のcorrectVoteSubmit内confirmDrop）は元々フロントに配線されていなかった
  「特別な達成の自動ドロップ」という別種のものなので変更していない。
- 新規`migrations/0019_character_points.sql`：`characters.character_points`列を追加し、
  `point_events`へのINSERTをフックする`trg_point_event_to_charpoints`トリガーで
  ルーティン行動4種のみ+1する。XP/レベルを`point_events`から自動生成する既存の
  `trg_point_event_to_xp`（migrations/0009）と全く同じパターンを踏襲し、
  `posts.ts`/`review.ts`のアプリケーションコード側は一切変更していない
  （ポイント付与はDBトリガー任せ）。トリガーは`AFTER INSERT`なので、マイグレーション適用前の
  既存の`point_events`には遡及しない（過去の行動は旧仕様で既にドロップ済みのため、
  遡及付与すると二重取りになる。意図的に不可）。
- 新規`POST /api/game/gacha/pull`（`src/game.ts`の`gameGachaPull()`）：body`{user_id,times}`
  （1か5のみ）。二重消費防止のため`UPDATE ... WHERE character_points >= times`を先に
  単独実行し影響行数0なら中断、その後`rollNormalDrop()`を`times`回・`user_characters`の
  既存所持species_idと比較して「初ゲット」を判定・`dropStatement()`をbatchで実行し、
  `{drops:[{species_id,name_key,rarity,is_new}],remaining_points}`を返す。
- `game.html`の`<section class="hero">`（`<a id="hero-link">`の外側。中に入れるとリンクの
  入れ子で誤動作するため）にキャラポイント表示と「キャラゲット」「キャラゲット×5」ボタンを新設。
  `public/gacha-reveal.js`は以前の`showGachaReveal(drop)`（自動フェードで消える単発カード、
  ルーティン4箇所の呼び出し元がすべて無くなったため）を`showGachaResults(drops)`
  （複数結果グリッド表示・明示的に閉じるまで残る・「キャラ図鑑を確認」ボタン・初ゲット種族への
  赤字点滅「NEW!」バッジ付き）に置き換えた。
- `report.html`/`judge.html`/`curate.html`のルーティン4箇所は`showGachaReveal(j.drop)`呼び出しを
  削除し、既存のポイントtoast/受領メッセージに`+1🎴`を追記するだけにした（もう使わない
  `import { showGachaReveal } ...`も削除）。
- `wrangler dev`で実データを使い、判定送信→`characters.character_points`が1増える→
  `POST /api/game/gacha/pull`でポイント消費・ドロップ発生・`is_new`判定・残高不足時のエラーまで
  一連の流れをAPIレベルで確認済み（テストデータは削除・本番未投入）。合成機能・図鑑表示・
  レア度抽選ロジック自体・XP/通常ポイント経済は変更していない。実ブラウザでのボタン操作・
  ポップアップ表示の確認はブラウザ操作ツールが使えず今回もできていない。

**（2026-08-29四改定）report.htmlのStep1「次へ」ボタンが撮影後に約5秒固まる不具合を修正**
（ユーザー指摘、2026-08-29）。原因は`refresh()`内でStep1の「次へ」を`state.aiChecking`（AI一次判定の
実行中フラグ）でも無効化していたこと。AI一次判定（Workers AI Visionモデルへの呼び出し）は
数秒かかる上、rule4により断定させない足切りに過ぎず、`createPost`側でも同じ判定を再実行する
多重防御構成（fail-open）になっているため、フロント側でこの完了を待たせる必要が無かった。
`.next-btn`の無効化条件から`state.aiChecking`を外し、写真のリサイズが終わった時点（`state.srcFull`が
入った時点）で即座に次へ進めるようにした。AI判定が後から「不適切」と判定した場合（低頻度）は、
既に先のステップに進んでいてもStep1へ強制的に引き戻して却下理由を表示するよう`analyzePhoto()`に
`showStep(1)`を追加し、取りこぼしが起きないようにした。`wrangler dev`でスクリプト部分の構文確認のみ
実施（AI判定の実際のレイテンシ体感はブラウザ操作ツールが使えず未計測）。

**（2026-08-29三改定）map.htmlに「← トップ」・カード単位のピン地図ビュー・いいね数表示を追加
（ユーザー指示、2026-08-29）**。
- 他ページと同じ`common.back_top`（「← トップ」）を、map.html独自のコンパクトなトップバー
  （`#topbar-row1`、他ページの`.app-topbar`とは別実装）にも表示するようにした。既存の
  ブランドマーク「T」の隣に小さく追記する形（スペースが限られるため、他ページのような
  2行スタック表示ではなく1行の小文字ラベルにしている）。
- カード詳細モーダルの「ここへ行く(Googleマップ)」リンク（外部アプリへの離脱）を廃止し、
  「地図で見る」ボタンに変更。押すとアプリ内で完結する新設のピン地図ビュー（`#pin-map-view`）に
  遷移し、そのカードの実際の緯度経度を中心に**東西南北1.5km（＝3km四方）のbboxでfitBounds**した
  Leafletの単一ピン表示になる（ズームレベル固定ではなくbbox指定なので、画面サイズに依らず
  常に3km四方相当が収まる）。Leaflet本体は「地図で見る」を押した時だけ動的ロードする
  （2026-08-29の前回改定でLeafletを完全撤去した一覧画面を重くしないため。CSSも含めて遅延注入）。
  「一覧に戻る」で一覧表示に戻る（Leafletインスタンス自体は使い回し、再訪時は
  `fitBounds`/マーカー差し替えのみ）。
- カードに「👍 いいね数」を追加表示（`/api/map`のピンプロパティに元々あった`like_count`を
  そのまま使用、バックエンド変更なし）。
- `googleMapsUrl`関数と未使用になった`map.goto_link`i18nキーは削除。新規`map.goto_map_btn`/
  `map.back_to_list`を6言語追加。
- `wrangler dev`で配信HTMLへの新要素混入を確認、bbox計算（中心35.681,139.767・halfKm=1.5）が
  実際に東西南北とも約2.997km（≒3km）になることをPython側で検算して確認済み。
  実ブラウザでのピン地図遷移・Leaflet描画自体は本セッションでは未確認（ブラウザ操作ツール不可）。

**（2026-08-29再改定）map.htmlのフィルタ表示・言語ラベル・カード位置表示を改善（ユーザー指示、2026-08-29）**。
- フィルタボタンの表示が「言語」で分かりにくかったため「フィルタ」に変更、アイコンも歯車(⚙️)から
  漏斗形のインラインSVGに差し替えた（絵文字に適切な漏斗が無いため、他のアイコンと違い唯一SVGを使っている）。
  i18nキーも実態に合わせ`map.lang_filter_btn`→`map.filter_btn`にリネーム。
- 言語ペアのラベル（`common.pair_label_*`、map.html/mypage.htmlで共用）を、「日→」プレフィックス＋
  1文字略語（例:「日→英」「日→仏」）から、フルの言語名（「英語」「フランス語」等）に変更。
  「日→」部分は全ペア共通で情報量が無く、1文字略語は言語によっては何を指すか分かりにくかったため。
  6言語ロケール全てで、その言語自身の言語名表記に翻訳し直した（例: en.jsは"English"/"French"、
  zh.jsは"英语"/"法语"）。
- カードに位置表示（「〇〇県〇〇区」粒度）を追加。postsテーブル自体は正確な緯度経度のみを持つ
  （rule1）ため、住所への変換は表示時にオンデマンドで行う：国土地理院の逆ジオコーダAPI
  （`mreversegeocoder.gsi.go.jp`、CORS許可済みを確認済み）で市区町村コードを取得し、
  新規`public/muni-codes.json`（geolonia/japanese-addresses由来の全国市区町村コード表、
  1894件・約76KBを圧縮済みJSONで同梱）で「都道府県名+市区町村名」に変換する。
  町丁目レベルの詳細（GSIレスポンスの`lv01Nm`）は使わず、依頼された粒度に留めている
  （精度を上げすぎると個別投稿の所在地が特定されやすくなるため、意図的に粗く保つ）。
  近傍の投稿は緯度経度を小数点3桁に丸めたキーでキャッシュを共有し、重複した外部API呼び出しを避ける。
  カード自体の表示はブロックせず、位置ラベルは解決でき次第非同期で追記する。
  `wrangler dev`で実際に`/api/map`のピン座標(35.681,139.767)を逆ジオコーダ→市区町村コード表の
  パイプラインに通し、「東京都千代田区」が得られることをエンドツーエンドで確認済み。

**（2026-08-29改定）UI改善バッチ（ユーザー指示の17項目リストのうち着手分）**：
- `menu.html`：キュレーションモード側の最下段にあった「マイページ」導線を削除（マイページは閲覧モード
  （`mypage.html`への別導線）で足りており、キュレーションモードの選択画面に置く必要が無かったため）。
- `common.arrow_lang_en/zh/ko`（judge.html・curate.html共通の言語フィルタボタン文言）から先頭の「→」を除去。
  6言語ロケール全ファイル＋judge.html/curate.htmlのdata-i18nフォールバックテキストの両方を修正。
- `curate.html`「修正・投票」タブの一発目の画面を再設計：同じ投稿に対し**他の誰かの修正提案が既にある場合は
  賛成/反対の投票UIを、まだ無い場合は「修正案がまだありません。最初の投稿者になりましょう！」
  （新規i18nキー`curate.be_first_prompt`）を添えた提案フォームを**、同一カード内で出し分けるようにした。
  バックエンド`src/review.ts`の`correctNext`が`existing_correction`（他者の`proposed`な修正案。
  相関サブクエリで1件だけ取得）をレスポンスに追加、フロントの`loadFixNext()`がこれで分岐する。
  投票は既存の`/api/correct/vote/submit`をそのまま再利用（`voteOnFixCorrection()`）。
- 「修正・投票」タブ（提案画面・投票画面の両方）に judge.html と同じ意匠のスキップボタンを追加。
  `correctNext`・`correctVoteNext`双方に judge.html と同じ`exclude`パラメータ（直近20件のIDをカンマ区切りで
  送り`NOT IN`化）を実装。クライアント側は`seenFix`/`seenVote`配列に積んで送る。
  ローカルD1にテスト用の投稿2件・修正提案1件を仮投入し、`existing_correction`分岐と`exclude`除外の両方を
  実APIレスポンスで確認済み（投入データはテスト後に削除済み・本番未投入）。
  この回のフロントエンド確認はブラウザ操作用ツールが使えなかったため、`wrangler dev`起動＋実HTTPリクエスト
  （curl）でのAPIレベル検証と、配信されたHTMLへの新規要素混入確認までに留めている。実ブラウザでのクリック
  動作確認は次回このファイルを触る際に併せて行うこと。
- **「その他」言語選択時のエラーを修正**：`report.html`の「その他」ドロップダウンは18言語を提示していたが、
  実際に`posts`テーブルのCHECK制約（`migrations/0010_expand_lang_pairs.sql`）が許可するのは
  en/zh/ko/fr/es/hiの6言語のみだった。従来の`isValidLangPair()`（`src/config.ts`）は緩い正規表現
  （`^ja-[a-z]{2,3}$`）でしか検証しておらず、未対応言語（独・伊・タイ語等）を選んで投稿すると
  API層のバリデーションはすり抜け、D1のCHECK制約違反で未捕捉の500エラーになっていた。
  `isValidLangPair()`を`LANG_PAIRS`（configの正式な許可リスト）による厳密チェックに変更し、
  同時に`report.html`/`judge.html`/`curate.html`3ファイルの「その他」ドロップダウンから、
  実際には投稿・保存できない12言語（独伊葡露アラビア語タイベトナムインドネシア語タガログ語
  ネパール語ベンガル語トルコ語蘭ポーランド語ウクライナ語）を削除し、fr/es/hiの3つに絞った
  （judge.html/curate.htmlはフィルタ用途でエラーにはならないが、選んでも永久に0件になる
  無意味な選択肢だったため合わせて整理）。`wrangler dev`＋curlで`ja-de`が400（クリーンな
  バリデーションエラー）に、`ja-fr`/`ja-en`が正常応答になることを確認済み。
- **`report.html`のUI改善2点**：①モード選択ボタン（「ヘンな訳文を撮影」/「外国語の映っている掲示物を撮影」）
  押下後、即座にカメラが起動するようにした（`chooseMode()`内で`showStep(1)`の直後に
  `$("camera-btn-src").click()`を呼ぶだけ。`bindPhotoButtons()`によるクリックリスナーは
  モジュール読み込み時点で既に張られているため、実行順序の問題は無い）。②モード選択ボタンを、
  menu.htmlの`.grid`/`.tile`と全く同じ意匠（正方形2列タイル、primary/map-accentの配色）に統一。
- **`map.html`を地図無しの純カードリストに作り替え**：直前の作業（list+map hybrid化、コミット`73425f5`）を
  さらに一歩進め、ユーザー指示（2026-08-29）でLeaflet地図そのものを撤去した。Leaflet CDN読み込み・
  `#map`/`#map-wrap`・ピン描画・パン検出（「この範囲を検索」ボタン）は全部削除。地図が無いので地域絞り込みは
  「地名検索」だけが担う：検索欄が空のときは`NATIONAL_BBOX`（`posts`の`lat`/`lng` CHECK制約と同じ日本全国の
  範囲）を対象にし、地名を検索するとGSI APIの結果点を中心に±0.05度のbboxへ切り替える。カードタップ時は
  以前Leafletポップアップに出していた内容（状態・言語・原文/訳文・日付・確定時の解説・いいねボタン・
  未確定時の判定/修正へのCTA・Googleマップ導線）を、新設の詳細モーダル（`#detail-overlay`/`#detail-modal`）に
  そのまま移設した。`mypage.html`からの`map.html`への導線・`game.html`「街を見る」からの遷移（クエリ無し）は
  従来通り機能する（このファイルの変更範囲外）。
- **審査中ステータスの表示だけ先取りする暫定リーン表示（rule12・DBスキーマ非変更）**：
  `posts.status`・`judgeSubmit`の閾値遷移ロジック（`JUDGMENT_THRESHOLD=2`）は一切変更していない。
  `src/map.ts`の`/api/map`ピンクエリに`unnatural_count`/`natural_count`（該当投稿の`judgments`集計、
  `pending_judgment`のときのみ意味を持つ）を追加しただけ。`map.html`の`displayStatus()`が、
  `status==='pending_judgment'`かつ票が1件以上入っていて多数決に差がある場合だけ「違和感あり/なし(暫定)」の
  色・ラベルに置き換える（新規i18nキー`map.status_leaning_unnatural`/`status_leaning_natural`、
  凡例に説明文`map.legend_leaning_hint`を追加）。まだ票が無い、または同数の場合は従来通り「審査中」。
  `wrangler dev`＋curlで、`judgments`に1件unnatural票を仮投入した投稿の`/api/map`レスポンスに
  `unnatural_count:1`が乗ること、`posts.status`が`pending_judgment`のまま変化しないことを確認済み
  （テストデータは確認後削除・本番未投入）。フロントの表示分岐自体は本セッションではブラウザ操作ツールが
  使えず実クリック確認はできていない（他のUI変更と同様、次回ブラウザで触れる際に確認すること）。
- **キャラゲット抽選演出を追加**：`src/posts.ts`/`src/review.ts`の投稿・判定・修正提案・投票の各エンドポイントは
  以前からドロップ自体は毎回サーバ側で抽選し`character_drops`に記録していたが（`src/game.ts`の
  `rollNormalDrop`/`rollRareDrop`）、何が当たったかをレスポンスJSONに一切含めていなかった（フロントは
  ポイント加算しか知りようがなかった）。`rollNormalDrop`/`rollRareDrop`が種族名(`name_key`)も一緒に返すよう
  拡張し、`dropPayload()`ヘルパーで`{species_id, name_key, rarity}`の形にして`/api/posts`・`/api/judge/submit`・
  `/api/correct/submit`・`/api/correct/vote/submit`のレスポンスに`drop`フィールドとして追加した。
  （`correctVoteSubmit`は確定ボーナス用の`confirmDrop`も内部で抽選するが、それは元の提案者への付与であり
  リクエスト送信者＝投票者のものではないため、レスポンスには投票者自身の`voteDrop`だけを返している。）
  フロント側は新規共有モジュール`public/gacha-reveal.js`の`showGachaReveal(drop)`を`report.html`（投稿成功時）・
  `judge.html`（判定送信時）・`curate.html`（修正提案・投票・修正案への賛否の3箇所）から呼ぶ。
  collection.htmlのレア度別背景色（r1〜r4のグラデーション）と`images/species/{id}.png`
  （無ければ`mon-placeholder.svg`にフォールバック）をそのまま流用し、種族名は`game.species.{name_key}.name`の
  既存i18nキーで解決する。判定・修正・投票は連続操作のキューなので、演出は`await`で止めず自動フェードアウト
  （レア度に応じ0.9〜2.4秒、タップで早送り可）にして、KPI上重視しているスループット・継続率を妨げないようにした
  （report.htmlの投稿だけは単発アクションなので、既存の完了モーダルに重ねて出す形）。
  `wrangler dev`＋curlで`/api/judge/submit`のレスポンスに`drop:{species_id:"fennec_fox",...}`が乗ること、
  対応するi18nキー（`game.species.fennec_fox.name`）が実在すること、report.html/judge.html/curate.htmlの
  配信HTMLに`gacha-reveal.js`のimportが実際に入っていることを確認済み（テストデータは削除・本番未投入）。
  実ブラウザでの演出そのもの（アニメーション・タップ早送り）はブラウザ操作ツールが使えず未確認。

**（2026-08-25改定）ゲーム層（`game.html`）の再設計**：2026-08-22時点では「クビアカ由来のキャラ育成ゲームは
翻訳ドメインと無関係のため削除」としていたが、その後の作業でLP（`index.html`）が`lp_mock.html`のデザインを
全面採用し、LEVEL UP/COLLECTセクションで「称号付きレベル・トロフィー・レアリティ付きキャラ収集ガチャ」を
約束する内容になった。実装とLPの約束を一致させるため、旧「相棒（1体育成）」システムを廃止し、複数種の
キャラクターを集める収集ガチャに置き換えた（ユーザー指示、2026-08-25）。
- `migrations/0012_character_collection.sql`：`species`（種族マスタ）・`character_drops`（ドロップ事実ログ、
  `xp_events`と同じ`source_key`冪等性パターン）・`user_characters`（所持数集計）を新設。`characters`に
  `last_level`列を追加してレベルアップ検知に使う。絵は当面`mon-placeholder.svg`を種族間で共用し背景色で
  レア度を表現する（lp_mock.html自身が同じ絵を4カードで使い回す方式に合わせた。個別絵は後回し）。
- ドロップは投稿・判定・修正提案・投票（`src/posts.ts`/`src/review.ts`の各`env.DB.batch`）に1文追加する形で
  フックし、ポイント付与とドロップが同一トランザクションでアトミックになるようにした。レベルアップ・
  修正確定はレア確定ドロップ、図鑑12マスコンプリート（クエスト）はmystery（★4）確定。
- mysteryキャラの本来のトリガーはlp_mock.htmlの文言どおり「街を直すと出会える」＝`status='adopted'`だが、
  ④店の採用フロー自体が未実装（次の作業1）のため、現状は図鑑コンプリートからのみ到達可能。採用フロー実装時に
  `adopted`遷移箇所へも同じドロップフックを足すこと。
- `users.streak_count`/`streak_at`/`streak_mesh5`（クビアカ時代の`migrations/0007`由来で0008に引き継がれたが
  誰も書き込んでいなかった列）を実際に稼働させた。投稿・判定のたびにJST日付で連続日数を更新する
  （`src/utils.ts`の`nextStreakCount`）。
- `game.html`にCOLLECT（種族収集グリッド）・MAPコレクション（自分の確定/採用投稿を小さなLeafletマップに表示）
  セクションを新設。既存の街ことば図鑑（言語×表記カテゴリ12マス）・クエスト・地域インパクトはそのまま維持
  （図鑑とキャラ収集は別概念として並存）。
- **（2026-08-25再改定・その2）** ユーザー指示により、game.html/mypage.htmlの役割分担をさらに整理した。
  `game.html`は「今のレベル・称号・XPバー・選択中キャラのスプライト・ストリーク・3アクションへの入口」だけを持つ
  軽量なステータス画面に縮小。クエスト・街ことば図鑑・地域インパクト・COLLECT（種族収集グリッド）・
  マップコレクションは全て`mypage.html`に移設・統合した：
  - クエストは見出しを「トロフィー」に変更し、mypage側では①アイコン列のみを常時表示、②「すべて見る→」で
    開くモーダル（`#trophy-modal`）内にクエストカード＋受け取るボタンのフル機能、という2階層構成にした
    （ユーザー指示の「もう一個深い階層に入れてOK」に対応）。
  - 街ことば図鑑（言語×表記カテゴリ12マスの視覚グリッド）はUIごと削除した。`zukan_12`クエスト
    （トロフィー内に残存）が実質的に同じ達成を代表するため、統合扱いとした。バックエンドの
    `GET /api/game/encyclopedia`自体は害がないため残してある（現在フロントからの呼び出しは無い）。
  - キャラ表示選択（`characters.display_species_id`列・`migrations/0013`・
    `POST /api/game/collection/select`、所持していない種族は選べない）は`game.html`のヒーロー専用機能として
    そのまま。実際の選択操作自体はmypage.htmlの街ことばコレクションカードの「この子を表示」ボタンで行う。
- **（2026-08-26改定）`public/collection.html`（種族タイル一覧ページ）を、将来100種類まで増やす前提の
  密なグリッドに作り直した（ユーザー指示、2026-08-26）**。
  - `.dex-grid`を5列固定（"5x20"想定）に変更。現状`species`は4件しか無いため実際に描画されるのは1行分だが、
    種族マスタが増えるほど自然に行が増える設計。100件分のダミー種族はこの作業では作らず、グリッド側だけを
    スケール対応させた（コンテンツ追加は別タスク）。
  - 未取得の種族はタイル・詳細モーダルの両方で「名前・★レア度・SPECIALバッジ」を伏せ、`???`表示に統一
    （従来はrarity>=4の`mystery`だけ`???`にしていたが、レア度自体を取得前に漏らさないよう全種族に拡張）。
  - 未取得タイルの絵は`filter:grayscale(1)`（色を抜くだけで輪郭は見えたままだった）から
    `filter:brightness(0) opacity(.45)`（純粋なシルエット化）に変更し、背景もレア度色ではなく
    一律グレー（`#dfe3de`）にした。取得済みタイルのみ、その種族本来のレア度背景（r1〜r4）と
    フルカラーの絵を表示する（＝「ゲット済みの実際のレア度で背景を出す」という指示を、
    既存の1種族=1レア度スキーマのまま満たす形で実装。同一キャラが複数レア度を持つような
    データモデル変更は行っていない）。
  - タイル面からは★レア度・所持数テキストを外し、アイコン＋名前のみの簡素な表示にした
    （5列の密なグリッドで文字が窮屈になるため。詳細はタップ後のモーダルに集約）。
- **（2026-08-26改定）ストリークをmypage.htmlにも表示し、マイルストーン報酬を追加した（ユーザー指示、
  2026-08-26）**。ストリーク自体（`users.streak_count`/`streak_at`の更新・game.htmlでの表示）は
  2026-08-25時点で既に実装済みだったため、今回はその2点の追加のみ：
  - `migrations/0014_streak_best.sql`：`users.streak_best`（過去最長ストリーク、途切れても減らない
    ハイウォーターマーク）を新設。`streak_count`は途切れると1にリセットされるため、これをマイルストーン
    実績の判定に使うと「あと少しで7日達成のところで1日空けてしまい、二度と`streak_7`が取れなくなる」という
    行き止まりを生む（rule3の「行き止まりを作らない」に反する）。`src/posts.ts`/`src/review.ts`の
    ストリーク更新箇所で`streak_count`と同時に`streak_best = MAX(streak_best, newStreak)`も書き込む。
  - `src/game.ts`の`QUESTS`（＝mypage.htmlの「トロフィー」）に`streak_3`/`streak_7`/`streak_14`/`streak_30`を
    追加。新規テーブルは作らず、既存のクエスト＝トロフィー機構をそのまま再利用（`questDone`に
    `streak_best >= N`の判定を追加しただけ）。`streak_7`/`streak_14`はレア確定ドロップ、`streak_30`は
    `zukan_12`と同じくmystery（★4）確定ドロップも同時付与する。
  - `judge_10`のアイコンを🔥から👀に変更（元々🔥は判定タスクの内容と無関係だった上、ストリーク4件と
    アイコンが重複するため）。
  - `mypage.html`の見出し下に`#streak-line`（🔥 N日連続）を新設。`/api/user/mypage`（`src/user.ts`の
    `mypage()`）のレスポンスにも`streak_best`を追加済み（現状フロント表示は`streak_count`のみ使用、
    `streak_best`は将来の「自己ベスト」表示用に温存）。
- **（2026-08-26改定）`index.html`の`<section id="hero">`をDownloadsに残っていた`lp_mock.html`の
  `<header class="hero">`と見た目が一致するよう作り直した（ユーザー指示、2026-08-26。`lp_mock.html`は
  リポジトリには無く`~/Downloads/lp_mock.html`にのみ存在）**。
  - フルブリード（`width:100vw`＋負のマージンで`.shell`の1080px制約を突き抜けさせる）の緑帯に変更。
    従来は角丸・box-shadow付きの「浮いたカード」型ヒーローだった。
  - 統計を3つに増やし（`集まった投稿`/`確定した修正`/新設の`街で直った数`）、枠なしのプレーンな見た目に変更
    （従来は半透明の縁取りカードだった）。3つ目の`adopted`件数用に`/api/health`のSQLに
    `SUM(status='adopted')`を追加。`status='adopted'`への遷移経路自体はまだ無い（次の作業1）ため、
    採用フロー実装まではこの数値は基本0のまま。
  - ヒーロー内に「🎮 Game On ›」ボタン（既存の`.game-btn`をそのまま流用）を新設し、末尾に
    スクロール誘導文言（bobアニメーション）を追加。
  - ヒーロー直下にあった独立の`<a id="buddy">`ブロック（マップピン装飾・クリック時の花火パーティクル演出・
    「Start finding and fixing mistranslations!」という副題）は削除し、上記のヒーロー内蔵CTAに一本化した
    （lp_mockのヒーローCTAは装飾のないプレーンな`.game-btn`1つのみのため、2つのGame On導線が縦に並ぶ
    見た目の重複を避けた）。花火パーティクル演出はこの整理で失われている＝要望があれば新CTAへの
    再実装を検討。floating-cta（下部固定のGame Onボタン）の表示トリガーは、削除した`#buddy`の位置基準
    から`#hero`セクション自体の位置基準に付け替えた。
  - lp_mockの`.hero-note`（「本日は工事中です。/ Today is under construction. / ← 二条城の実物」という
    プレースホルダーのミニ看板）はそのままでは移植していない。理由：index.htmlはこの日名前由来ネタを
    既にWHYセクション側（`index.why_sign_ja`/`index.why_sign_cap`＝「← これがTIUCという名前の由来です」）
    に持たせ済みで、ヒーロー側は本物のブランドロゴ画像（`images/title-image.png`、当日作成）に
    差し替え済みだった。lp_mockのプレースホルダーへ巻き戻すと実ブランド資産を捨てることになるため、
    「見た目を一致させる」対象を外枠（フルブリード・統計・CTA・スクロール誘導）に絞り、ロゴ画像自体は
    現状のまま維持している。
- **（2026-08-26再改定）上記の「ロゴ画像を残す」判断を、ユーザー指示により撤回し、`hero-art`を
  lp_mockの`.hero-note`（ミニ看板）にそのまま差し替えた（ユーザー指示、2026-08-26）**。
  - 前回のCLAUDE.mdの記述は誤認だった：WHYセクションの`.fake-sign`（「本日、二の丸庭園の滝は工事に伴い
    水を流しておりません。」→「TODAY IS UNDER CONSTRUCTION」）と、ヒーローの`.hero-note`
    （「本日は工事中です。」→「Today is under construction.」）はlp_mock内でも**別々の実在する2つの例**
    であり、WHYセクション側に既にあるからヒーロー側は不要、という前回の判断は誤りだった。実際には
    ヒーロー側の小ネタはindex.htmlにまだ一切実装されていなかった。
  - `images/title-image.png`（ブランドロゴ）はヒーローから削除。ファイル自体は削除せず`public/images/`に
    残してある（他所での再利用に備えて）が、現状どのHTMLからも参照されていない。
  - `.hero-sign`/`.hero-sign-board`/`.hero-sign-legs`/`.hero-sign-ground`（画像用に組んでいたCSS）は
    `.hero-note`/`.board`/`.bd`/`.ja-line`/`.sign`/`.cap`/`.legs`/`.ground`（lp_mockのクラス名そのまま、
    値もlp_mockの数値をそのまま採用）に置き換えた。
  - 看板の日本語原文（`index.hero_note_ja`＝「本日は工事中です。」）と英訳（`.sign`＝
    "Today is under construction."、i18n化していない）は、WHYセクションの`why_sign_ja`/`.fake-sign .en`と
    同じ方針で言語切り替えの対象外にしている（「実在する看板とその誤訳」という実演コンテンツなので、
    UI言語が変わっても内容自体は変えない）。注釈の`index.hero_note_cap`（「← 二条城の実物。何がヘンか、
    わかる？」）は他の注釈と同様に6言語分翻訳した。
  - lp_mock本体に合わせ、`.hero-note`は`max-width:720px`で非表示（モバイルでは看板を出さない）。
- **（2026-08-27改定）キャラ図鑑を100種類まで拡張する第一弾として60種を追加した
  （`migrations/0015_species_roster_60.sql`、ユーザー提供のCSVがベース）**。
  - 新方針が明らかになった：基本種はすべて`rarity=1`（★1）で入手し、**合成**（★1→★2→★3、未実装）で
    ランクを上げる。★4は合成では到達できない個別の特別キャラ専用の枠（`mystery`が最初の例で、以後も
    同じ枠に追加していく想定）。そのため旧来の「種族ごとに固定レア度を割り当てる」設計
    （sparrow=★1・white_eye=★2・kingfisher=★3）は廃止。
  - ユーザーからの回答「ユーザーいないから破壊的操作してOK」（2026-08-27）を受け、本番の
    `character_drops`/`user_characters`のうち該当データ（テストユーザー2名分、計6件のみ）を削除して
    実施。`white_eye`は新リストの`white_eye_bird`に統合されるためid自体を削除、`kingfisher`はidを
    維持したままレア度だけ1に更新（所持記録は無傷）。`sparrow`・`mystery`は変更なし。
  - `src/game.ts`の`NORMAL_DROP_TABLE`/`RARE_DROP_TABLE`（固定4種時代の重み付けハードコード配列）を
    廃止し、`rarity=1`の`species`テーブルを都度クエリして均等抽選する方式に変更（`rollNormalDrop`/
    `rollRareDrop`が`env`を取るasync関数になったため、`posts.ts`/`review.ts`の呼び出し箇所5箇所に
    `await`を追加）。**`rollRareDrop`は合成メカニクス未実装のため、現状`rollNormalDrop`と同じ
    基本種プールを引く暫定実装**（合成が実装されたらここを差し替える。TODO）。
  - 画像は全種`mon-placeholder.svg`共用のまま（実イラストは未着手。ユーザーが順次用意する予定）。
    `collection.html`/`game.html`は`species`テーブルを汎用的に描画しているため、ロースターが
    4種→61種に増えてもフロント側の追加改修は不要だった。
  - i18n：`game.species.<id>.name`/`.desc`を6言語×59種ぶん追加（`sparrow`は変更なしなので追加対象外）。
  - 名前・説明・画像ファイル名は当面ユーザー提供のCSV（`id,sort_order,rarity,name_ja,desc_ja,
    image_filename`形式）で受け渡す運用。実イラスト画像は`public/images/species/`に置く想定
    （R2は使わない。投稿写真と違いアクセス制御不要な静的アセットのため）。
  - 同日中にユーザーが実際に5種分のイラスト（`long_tailed_tit`/`Pallas_cat`/`axolotl`/`red_panda`/
    `T_rex`）を`public/images/species/`に用意したため、表示側も実装した：
    `collection.html`（タイル・詳細モーダル）と`game.html`（ヒーローの選択中キャラ表示）で、
    `images/species/${id}.png`を試みて404なら`onerror`で`mon-placeholder.svg`にフォールバックする方式。
    画像ファイル名は**`{id}.png`（数字プレフィックス無し）に統一**した
    （ユーザーが最初に置いた5枚は`{sort_order}_{id}.png`形式だったためリネーム済み。
    表示順は`sort_order`カラムが担うのでファイル名に含める必要が無く、並び替え時に
    ファイル名を変更しなくて済むメリットもある。今後の画像追加もこの命名で）。
    実イラストは正方形とは限らないため`.mon`に`object-fit:contain`を追加し、
    `image-rendering:pixelated`（プレースホルダー専用の見た目）は外した。
- **（2026-08-27再改定）LPのCOLLECTセクションのサンプル4枚を実キャラ画像に差し替えた
  （ユーザー指示、2026-08-27）**。★1=ウーパールーパー(axolotl)・★2=レッサーパンダ(red_panda)・
  ★3=シマエナガ(long_tailed_tit)・★4=ティラノサウルス(T_rex、SPECIAL枠)。
  ユーザーの意図：「同じキャラで背景色だけ変わる」ように見えると「結局背景が変わるだけのゲームか」と
  誤解されるため、**あえてキャラ自体も背景も変えた組み合わせ**をLPに載せる。実際のレア度は
  種族固定ではなく合成で決まる（2026-08-27の別改定参照）ため、この4枚の割り当ては一例に過ぎない。
  名前の`data-i18n`は新規キーを作らず、既存の`game.species.<id>.name`（60種追加時に用意した6言語分）を
  直接参照する形にした（`index.mon1_name`等の旧キーは削除。二重管理を避けるため）。
- **（2026-08-27再々改定）合成メカニクスを実装した（`migrations/0016_synthesis.sql`、ユーザー指示、
  2026-08-27：「レア度1→2は2枚、レア度2→3はレア度2を3枚で合成可能。ドロップ率はレア度1が80%、
  レア度2が15%、レア度3が4.5%、レア度4は0.5%」）**。
  - **所持数をレア度ティア別に管理する必要が生まれた**（同じ種族を★1のまま2体持ちつつ、既に合成した
    ★2も1体持つ、という状態がありえるため）。`user_characters`の主キーを`(user_id, species_id)`から
    `(user_id, species_id, rarity)`に変更（SQLiteは主キー変更にテーブル再作成が必要なため
    `character_drops`ごと作り直し）。`character_drops`にも`rarity`列を追加。テストユーザーの
    既存所持データ（本番でごくわずか）は今回も破壊的に作り直した。
  - `src/game.ts`の`rollNormalDrop`/`rollRareDrop`を全面刷新：`{tier:80, tier:15, tier:4.5, tier:0.5}`の
    ティア抽選→★1〜3は共通の基本種プール(60種)から、★4は専用の特別枠プール(mystery等)から種族を選ぶ
    方式に変更。戻り値も種族idの文字列から`{speciesId, rarity}`に変更したため、呼び出し元
    (`posts.ts`/`review.ts`の4箇所、`game.ts`内2箇所)を全て更新。`rollRareDrop`（レベルアップ・
    修正確定ボーナス）は★1を除外して再抽選する「確定で少し良い」枠として実装（旧TODOを解消）。
  - **これで「ゲット済みの最大レアリティの背景を表示する」機能が実質的に意味を持つようになった**
    （2026-08-26のcollection.html密グリッド化の際に「将来複数レア度バリエーションを追加するなら
    再設計が要る」と保留していた論点。今回の合成実装でまさにその状態＝同じ種族を複数ティアで
    同時に持つ、が実現した）。`gameCollection`のレスポンスの`rarity`フィールドは
    「未所持なら種族の基本レア度、所持していれば合成込みの最高到達ティア」を返すよう変更、
    加えて`tiers`（ティア別所持数の内訳）を新設。フロントの`collection.html`は`r${s.rarity}`を
    そのまま使い続けられるため、タイルの背景・☆数ロジック自体は無改修で最高ティア表示に対応した。
  - 新規`POST /api/game/synthesize`（body: `user_id, species_id, from_rarity`）。指定ティアの所持数が
    必要数（1→2は2、2→3は3）以上あるか確認し、消費して1つ上のティアを+1する。
  - `collection.html`の詳細モーダルに「所持内訳」（ティア別内訳）と、条件を満たすティアがあれば
    「{from}を{n}体使って{to}に合成」ボタンを追加。合成後は`/api/game/collection`を再取得して
    タイル・モーダルを最新状態に更新する。
  - `gameSelectDisplayCharacter`（表示キャラ選択）・`gameCharacter`の`display_rarity`
    （game.htmlヒーローの背景色決定）も、単純な`species.rarity`参照から
    「そのユーザーがそのキャラで実際に到達した最高ティア」を見るよう修正した
    （直さないと、合成前の低ティアのまま背景が変わらなくなるバグになるところだった）。
- **（2026-08-27改定）撮影投稿フローにAI画像認識を追加し、UXフローも変更した（ユーザー指示、2026-08-27）**。
  このアプリで初めて`env.AI`（Workers AI）を実際に呼び出した（rule4「AI判定は断定させない・足切りのみ」
  の実装。新規`src/moderation.ts`の`analyzeSignPhoto`に集約）。
  - モデルは`@cf/meta/llama-3.2-11b-vision-instruct`。**初回利用時、アカウント単位でMetaのコミュニティ
    ライセンスに`prompt:"agree"`で同意する一度きりの手続きが必要**だった（同意前は`InferenceUpstreamError
    5016`で失敗する。fail-open実装のおかげでエラー自体はアプリを壊さなかったが、同意するまでは
    足切り・言語/種別デフォルト提案が常に「判定なし」になっていた）。同意は本番アカウントでも
    済ませてある。
  - `env.AI.run()`の戻り値は`{response: {...}}`で、`response`は**文字列ではなく既にパース済みのJSON
    オブジェクト**だった（プロンプトでJSON形式を指示しただけだが、モデル側がオブジェクトとして返した）。
    `analyzeSignPhoto`はオブジェクト・文字列どちらで来ても処理できるようにしてある。
  - 1回のVision呼び出しで①不適切写真（グロテスク・セクシャル・顔だけ・性器だけ等）の判定、
    ②訳文言語のざっくり判定（en/zh/ko/other/noneの5値まで。それ以外の言語の細かいマッピングは
    精度が低いため見送り）、③表記種別の判定（menu/sign/notice/other/unknown）をまとめて行う。
    ①はハードブロック（上書き不可、写真がうまく認識できなかった旨の一般的な文言を返す。
    具体的な分類理由はUIに出さない）、②③はStep2/3のデフォルト選択に使うだけで、ユーザーが変更できる。
  - 新規`POST /api/posts/analyze`（撮影直後にサムネイルを送って事前チェック）に加え、`createPost`
    本体でも同じ判定を再実行する多重防御構成（事前チェックAPIをスキップして直接投稿APIを叩かれても
    弾けるようにするため。最終送信の遅延は1回分増える）。AI呼び出し自体が失敗した場合は
    fail-open（足切りをスキップして投稿は通す。継続率を守る方針）。判定結果はDBに保存しない
    （一時的な提案に過ぎないため）。
  - UXフロー変更：Step3にあった「これ、訳が変かも？と思った(任意)」チェックボックスを廃止し、
    撮影の一番最初（番号なしの入口画面）に「ヘンな訳文を撮影」/「外国語の写っている掲示物を撮影」の
    選択ボタンを新設。前者を選ぶと`flagged=1`になり、Step3の下に「4. なんて書いてある？」
    「5. 何がヘン？」という自由記載欄（`posts.what_it_says`/`whats_weird`、新規列）が表示される。
    後者では`flagged=0`でこの2欄も出ない。既存のStep「4. 位置情報」「5. メモ」はそれぞれ
    「6.」「7.」に番号を繰り下げた（ウィザードのページ数自体は4のまま。入口画面は番号に含めない）。
  - ついでに見つかったモバイル横スクロールのバグ（投稿完了直後だけページが横スクロールできる）も
    修正。原因は`#points-badge`（`min-width:max-content;white-space:nowrap`）が`#topbar-actions`の
    中で縮まず、投稿成功時に`loadPointsBadge()`がポイント/レベル文字列を書き換えるタイミングで
    狭い画面幅だと`.app-topbar`のflex行を押し出していたこと。`tokens.css`の`html.app-html`/
    `body.app-body`に`overflow-x:hidden`を追加（index.html/mypage.htmlも読み込む共通ファイルだが
    副作用は無いことを確認済み）、`#points-badge`も`.brand-copy`と同じ縮む実装
    （`min-width:0;overflow:hidden;text-overflow:ellipsis`）に変更した。
- **（2026-08-28改定）公開マップ`map.html`をスマホ版Googleマップ風の「リストが主・地図はフィルタ」型に
  再設計した（ユーザー指示、2026-08-28。対象は`game.html`の「街を見る」から辿る全ユーザー投稿の公開マップで、
  マイページの「マップコレクション」ミニ地図＝自分の確定/採用投稿だけの小さな地図、とは別物）**。
  - 画面を上下2段に変更：上段は固定高さ(42vh)の地図（個別ピンのみ、確定/採用投稿は無条件で写真も見える
    従来ルールのまま）、下段は投稿ごとのカードが縦にスクロールするリスト（サムネイル・状態バッジ・
    言語ペア・原文抜粋・日付）。地図のピンとリストのカードは同じAPIレスポンスを共有しており、
    カードをタップすると地図がそのピンへパン/ズームし、ピンクリックと同じLeafletポップアップが開く。
  - 「地図情報はフィルタで絞るときに使う」を素直に実装：地図をパン/ズームしても自動では再検索しない。
    最後に検索した位置から一定距離動くとGoogleマップ同様の「この範囲を検索」ボタンが浮かんで出て、
    押した時だけ新しい範囲で再検索する（地名検索は従来通り検索後に自動で1回だけ検索する）。
  - メッシュ集計の色分け・未投稿エリアの網掛け表示は撤去した（「最初から地図にマップする必要はなくてOK」
    に対応。バックエンドの集計SQL自体は削除せず残してあるので、将来ヒートマップ的な見せ方を
    復活させたい場合はフロント側だけ手を入れれば済む）。
  - `src/map.ts`の`/api/map`にステータスフィルタ（`status`パラメータ、新設の
    `MAP_POST_STATUSES`定数で検証、デフォルトはauto_rejected以外の5種類全部）と、
    ページネーション（`limit`既定30・上限100、`offset`、レスポンス直下に`has_more`）を追加した。
    ピンクエリに`ORDER BY event_at DESC`も追加（従来は順序保証が無かった）。
  - ついでに気づいたラベルの誤り（`needs_fix`＝修正案募集中の投稿が、`pending_judgment`＝審査中と
    同じ「審査中」ラベルを表示していた）を修正し、`needs_fix`には元々存在したが未使用だった
    `map.status_fix_proposed`（「修正案あり」）を正しく割り当てた。これはピンの色分け・凡例・
    新設のステータスフィルタ・カードバッジすべてに反映される。
  - `mypage.html`の「マップコレクション」ミニ地図カードの下に、`.collection-btn`と同じ見た目の
    ボタン「みんなの投稿を地図で見る」を新設し、この再設計後の`map.html`へリンクした（従来はここから
    公開マップへの導線が無かった）。`game.html`の「街を見る」からの既存リンク（クエリ無し）は
    そのまま動作する。

**動いているもの**

- `migrations/0008_translation_domain.sql`: reports→postsの作り替え完了。users / posts / quality_checks /
  judgments / corrections / votes / levels（スキーマのみ） / gold_items / point_events を実装。tiuc-db（新規D1）・
  tiuc-photos（新規R2）に適用済みでkubiaka-dbとは分離されている。
- ①撮影投稿：`report.html` + `/api/posts`。外国語表記の写真は必須・日本語原文の写真は任意（1枚目投稿後に
  「日本語版の写真もありますか？」で追加を促す）・言語ペア（ja-en/ja-zh/ja-ko）・
  表記種別（任意）・「変かも」フラグ（任意）・位置情報（EXIF/GPS/地図タップ）・近隣重複チェック。AI一次フィルタ（OCR）は
  未実装のため、投稿は常にpending_judgmentから始まる（auto_rejectedは無い）。
- ②違和感チェック：`judge.html` + `/api/judge/next|submit`。一タップ・キュー・言語ペア別。2票の多数決で
  needs_fix / looks_okに自動遷移（固定しきい値。適応難易度は後発）。
- ③正誤・修正・解説：`curate.html` + `/api/correct/next|submit`, `/api/correct/vote/next|submit`。
  「修正が必要/実は問題なし」の2択、原文・訳文の書き起こし（OCR代わりに人力）、投票2票で確定。
  確定した修正訳は`gold_items`に自動昇格する（rule8の「正解の自己増殖」を実装済み）。
- 閲覧モード：`map.html`（地図無しの純カードリスト。投稿直後から全ステータスをステータス別バッジ付きで
  カード表示・タップで詳細モーダル。地域絞り込みは地名検索のみ。2026-08-22改定のrule1参照）、
  `mypage.html`（貢献履歴・ポイント台帳・自己削除）。
- 画像配信の認可：投稿者本人 / ②のキュー経由（judge_id・サムネイルのみ） / ③のキュー経由（curator_id・フル解像度）
  に限定。認証なし直リンクは401（rule1）。
- ゲーム層：`game.html` + `/api/game/character|quests|quests/claim|collection|map-spots|encyclopedia|impact`。
  称号付きレベル・トロフィー（クエスト流用）・種族収集ガチャ・ストリーク・マップコレクション・街ことば図鑑・
  地域インパクト。詳細は上の「現在地（2026-08-25改定）」参照。
- 運営向け管理画面：`admin.html` + `/api/admin/*`。投稿の一覧/検索・詳細閲覧・手動修正
  （原文/訳文/状態/言語ペア。状態は正規の遷移ガードを迂回して任意の値に設定可）・削除（R2画像も削除）、
  リーダーボード（`points_total`降順）とポイント手動調整（`point_events`にkind='manual'で記録）、
  アクティビティログ（`point_events`のタイムライン）とページビュー集計（`page_views`、
  各ページの`track.js`ビーコンから自己申告）、キャラクタ（種族）の新規登録とイラストアップロード
  （`species`テーブルへの行追加＋R2の`species/{id}.png`、配信は`GET /species-img/{id}`）。
  すべての破壊的操作は`admin_audit_log`に記録する（migration `0018_admin.sql`）。
  種族の表示名・説明文（i18n）は引き続き静的ロケールファイル側の追記が必要（詳細は「認証」章参照）。

**意図的に削った・後回しにしたもの**

- 旧レビュアー管理者ログイン（`/api/review/*`・`reviewers`テーブル）は削除。画像アクセス制御は
  ②③のキュー参加（judge_id/curator_id）ベースに置き換えた。
- ④管理者（店）の「採用」API・UIは未実装（`status='adopted'`へ遷移する経路が無い）。rule11のループclosingは次回。
- levels（実効レベル・ヒステリシス・降格通知）はスキーマのみで、重みは全員一律1.0のまま。
- 道場・OCR前処理・適応難易度（判定の分散から難易度を出す）はCLAUDE.md本文が明言する通り後発機能。

## 次の作業（上から順に。一度に一つ）

1. ④店の「採用」フロー（受動/能動の入口・採用API・報酬の一斉還元）。
2. levelsの実装（重みの計算・display_rankのヒステリシス・降格時の道場誘導）。
3. quality_checks（誰でもサブモード）の専用UI（現状はテーブル/API未使用）。
4. 道場（gold_itemsを使った練習・実効レベルの引き上げ）。
5. OCR前処理（Workers AIで原文/訳文の下書き抽出。断定はさせない）。
6. 適応難易度（判定の分散から難易度帯を算出。データが溜まってから）。

## AI コーディングエージェントと進めるときのコツ（Claude Code / opencode 共通）`[流用/変更]`

このファイルは Claude Code だけでなく opencode も（`AGENTS.md` が無い限り）読む
プロジェクト共通のルールブックとして扱う。ツール固有の暗黙ルールに頼らず、
必要な手順はここに明文化する（暗黙ルールは別ツールには効かない）。

- 一度に一つのタスクに絞る。CLAUDE.md の「次の作業」を上から一つずつ。
- 差分を必ず自分の目で見る。特に**公開経路に座標や店特定情報が漏れていないか**毎回確認。
- 「そもそもこの機能を入れるべきか」「都・店とどう握るか」の設計判断は、
  この文脈を持つチャット側で相談してからコードに来ると事故りにくい。

### 実装完了の定義（DoD）— 「動いた」と報告する前に必ず確認する

2026-08-22、新規ファイル `report.js` にボタンのクリックハンドラを書いたが、
`report.html` から `<script>` で読み込むのを忘れて孤立ファイルになり、
ボタンが完全に無反応のまま「実装完了」として報告された事故があった。
再発防止のため、以下を機械的なチェックリストとして毎回実施する。

1. **新規ファイルは孤立させない**：JS/CSS/画像などの新規ファイルを作ったら、
   それを読み込む側（`<script src>`・`<link>`・`import` 等）が実際に追加されているか
   `grep` 等で確認する。「ファイルを作った」で終わらせない。
2. **UI/フロントエンド変更は実際に動かして確認する**：`npx wrangler dev` を起動し、
   対象の画面をブラウザ（またはヘッドレスブラウザ／Playwright）で実際に操作し、
   ボタン押下・フォーム送信などの golden path が動くことを目で確認してから
   「完了」と報告する。コードを読んで「動くはず」で止めない。
3. 確認できたら `wrangler dev` を止め、良ければ git push した上で
   `npx wrangler deploy` を実行して本番反映まで行う（このプロジェクトでは自動デプロイが
   効かないため、push だけでは終わらない。「開発環境」章を参照）。
