# ヨソモン 改善実装スペック（Claude Code 用）

このファイル1枚で、UI刷新とゲーム性強化をまとめて実装するための指示書。
渡し方：リポジトリ直下に置き、Claude Code に「このスペックを読んで実装して」とだけ伝える。

## 作業スタイル（トークン節約・速度優先）

- まずリポジトリ全体を読み、技術構成（Worker のルーティング / データ保存が D1・KV・その他か / 既存 HTML・CSS・JS / XP・ポイントの現状ロジック）を把握してから着手する。推測で作り直さない。
- 不明点は質問で止まらず、合理的な既定値で進める。置いた前提は1行ずつ記録する。
- 最初に「構成の把握結果＋各タスクへの対応を示す短い計画」を出し、そのまま最後まで一気に実装する。冗長な説明は省く。
- 既存コードを最大限流用。無駄な再読み込み・全面書き換えを避け、編集は差分最小で。

## 絶対に壊さない前提

- プライバシー：表示はサムネのみ／位置・投稿者情報は非表示／最終判定は自治体。
- ログイン不要・匿名で使える。
- Cloudflare Workers 構成を維持。新しいビルドシステム・重量級フレームワーク・不要な npm 依存は入れない（既存のバニラ構成を踏襲）。
- DB スキーマ変更は必要最小限。変更する場合はマイグレーションと後方互換を用意。

## 前提の設計決定（変えたいならこの節だけ先に書き換えてから渡す）

- 世界観：育つキャラ＝在来種の「味方バディ」（例：木を守るキツツキ）。外来種「ヨソモン」は倒す／記録する「敵・図鑑対象」側に置く。目的（駆除）とプレイヤー感情の向きを一致させる。
- キャラの本番用イラストは作らない。プレースホルダのまま、あとで差し替え可能な形にしておく。

## 実装タスク（この順。各項の「完了条件」を満たすこと）

### A. 世界観の統一

- 全画面のコピー・alt・ラベルを「相棒を育てる＝味方」「ヨソモン＝記録／駆除対象」で一貫させる。
- 兆候（フラス・成虫・脱出孔・見たが異常なし）と樹種を「ヨソモン図鑑」の記録対象として扱う土台を作る。
- 完了条件：トップ／通報／マイページで、味方と敵の向きが矛盾なく表現されている。

### B. コアループ拡張

- 図鑑：見つけた兆候・樹種を種類として記録し、コンプ率を表示。
- クエスト：週次／季節。虫の生態に連動（例：6–8月の成虫発生期に成虫を報告）。達成で XP・報酬。
- 定点観測ストリーク：同じ木／地点の再訪報告に連続ボーナス（モニタリング価値が高い行動を習慣化）。
- 完了条件：図鑑が埋まる・クエストが1つ以上回る・ストリーク加算が動作する。

### C. 報酬は「量」でなく「正しさ」に

- 段階報酬：送信＝小 XP、自治体が承認＝大ボーナス。
- 信頼度／ハンターランク：的中率で上下。写真 AI 判定があれば確信度をスコアに反映（無ければフックだけ用意）。
- スパム抑止：同一地点・短時間の連投や重複はディスカウント。
- 完了条件：承認状態で報酬が変わり、信頼度が可視化される。

### D. UI 刷新

- 通報をステップ式＋進捗バーに。写真ファーストの流れ。
- 選択肢（フラス・成虫・脱出孔…）をアイコン付きの大きなタップカードに。
- デザインシステム：差し色1色・広めの余白・角丸カード。色／間隔／角丸を CSS 変数でトークン化。マスコットを常駐させる。
- 手応え：XP 獲得アニメ、レベルアップ演出（紙吹雪＋効果音＋触覚）、送信成功のマイクロインタラクション。
- 空状態：マイページ等の空履歴を、初回行動を促すデザインに。
- 完了条件：モバイル幅で崩れず、デザイントークンで統一され、主要操作に視覚フィードバックがある。

### E. 貢献の可視化＆ゆるい社会性

- 地域インパクトメーター：市区町村単位で「今月○件を早期発見」。
- ローカルランキング：町内会／学校／市区町村単位（"自分の町で1位"が狙える粒度）。
- シェア：レベルアップ／図鑑カードを画像化して X・LINE に共有。
- 対応通知：自治体対応時に通報者へ「1本処置されました」等のフィードバック（ステータス連動、無ければ手動入力フックだけ）。
- 完了条件：集計が表示され、共有導線が動作する。

## 完了時に必ず出すもの

- 変更点の要約、置いた前提の一覧、追加／変更したデータスキーマとマイグレーション手順、手動テスト手順（README か PR 説明に追記）。

## やらないこと（スコープ外）

- バックエンドの作り直し／認証の必須化／DB の全面刷新。
- 重量級フレームワーク・SPA 化・ネイティブアプリ化。
- 本番用キャライラストの量産。
