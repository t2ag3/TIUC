# コントリビューションガイド

TIUC の開発に参加する人向けの決めごとです。短く保ちます。

プロジェクトの目的・スケジュール・ロードマップはチームのNotionを参照してください。

## タスクの取り方

1. [Issues](https://github.com/t2ag3/TIUC/issues) から取りたいものを選ぶ
2. **自分をアサインする**（許可は要りません。早い者勝ち）
3. 初めての人は `good first issue` ラベルから

やりたいことがIssueに無ければ、自分でIssueを立ててから着手してください。
「Issueを立てるほどでもない小さな修正」はIssue無しでPRを出してOKです。

## ブランチとPR

**PRは義務ではありません。main に直接 push してOKです。**
レビューが欲しいとき、大きい変更のとき、プレビューURLで確認したいときにPRを使ってください。

```bash
# 小さい変更
git switch main && git pull
npx wrangler dev   # 実際にブラウザで動かして確認
git push           # → 型チェックが通れば自動で本番へ

# レビュー・プレビューが欲しいとき
git switch -c feat/なにをやるか
git push -u origin feat/なにをやるか
# PRを作ると、プレビューURLがコメントされます
```

- ブランチ名は `feat/` `fix/` `docs/` `chore/` で始める
- PR本文に `Closes #12` と書くとマージ時にIssueが自動で閉じます
- PRの説明には「何を変えたか」と「どう確認したか」を書く。実装の経緯もここに書いてください（`CLAUDE.md` には書かない）

### PRにしたほうがいい場面

強制はしませんが、次のときはPRを使うことを勧めます。**戻すのが難しい変更だからです。**

- `migrations/` を触るとき（**revertで戻りません**。下記参照）
- 座標・画像の公開範囲を変えるとき
- 他の人が持っている領域のファイルに手を入れるとき
- 自分でも「これ大丈夫かな」と思ったとき

### レビュー

- **24時間以内にレビューする。** これはチーム全体の速度に直結します
- **24時間経ってレビューが付かなければセルフマージしてよい。** 止まるほうが損です
- レビューは「動くか」と「公開経路に座標や店の特定情報が漏れていないか」の2点を最優先で見る。書き方の好みは後回し

## デプロイ

**自動です。** main が更新されると、GitHub Actions（`.github/workflows/deploy.yml`）が
型チェック → 本番D1へのマイグレーション適用 → `wrangler deploy` を実行します。
直接 push でもPRマージでも同じ。状況は Actions タブで見られます。

**型チェック（`tsc --noEmit`）が落ちたら、そこで止まって本番には反映されません。**
main への直接 push を許可しているぶん、これが最後のゲートです。

### 壊してしまったとき

```bash
git switch main && git pull
git revert <壊したコミットのハッシュ>
git push          # → 自動で元の状態が本番にデプロイされます
```

**「壊したら戻せる」ので、直接 push を怖がる必要はありません。**

### ただしマイグレーションは戻りません

**`migrations/` に置いたSQLは、mainに乗った瞬間に本番D1へ適用されます。**
コードは revert で戻せますが、**適用済みのマイグレーションは revert では戻りません。**
列を消すSQLを流したら、そのデータは消えたままです。

破壊的な変更（テーブル再作成、列削除など）を書くときは、PRにして誰かに見てもらってください。
ここだけは直接 push しないほうが安全です。

### 手で叩きたくなったとき

緊急時やActionsが不調なときは手動でもできます（Cloudflareのチームに入っていれば誰でも）。
ただし**必ず main から**。

```bash
git switch main && git pull      # 必ずこれをやってから
npx wrangler deploy
```

`wrangler deploy` はローカルの作業ツリーをそのまま本番に上げるため、作業ブランチから叩くと
コミットしていない変更まで本番に乗ります。また **古いローカルmainから叩くと、
Actionsがデプロイした最新版を黙って巻き戻します。** 誰も気づかないので、これが一番怖い事故です。
手動で叩いたときは、チャットに「手動デプロイしました `a1b2c3d`」と一言流してください。

> ハッカソン特典の Cloudflare Paidプラン相当は原則2026年9月末までですが、
> Final Stage進出チームは継続して使えます。
> 進出できなかった場合は無料枠に戻るので、無料枠に収まる設計自体は維持してください。

## 完了の定義（DoD）

「動いた」と言う前に、毎回これを確認してください。

1. **新規ファイルを孤立させない。** JS/CSS/画像を追加したら、読み込む側（`<script src>` / `<link>` / `import`）が実際に入っているか `grep` で確認する
2. **UI変更は実ブラウザで操作して確認する。** `npx wrangler dev` を起動してボタンを押す。「コードを読んで動くはず」で止めない
3. i18nキーを追加したら6言語すべてに追加する

## 触る前に知っておくこと

詳細は `CLAUDE.md`。事故に直結するものだけ抜き出します。

- **座標を公開経路に漏らさない。** DBは正確な緯度経度を持ち、出口で粒度を変える設計です
- **メッシュコードはサーバ側で必ず再計算する**（クライアント送信値を信用しない）
- **AIに訳の正誤を断定させない。** AIは足切りと下書きまで。判定と修正は人間
- **AI呼び出しは fail-open。** AIが落ちても投稿は止めない
- **秘密の値を `wrangler.jsonc` に書かない。** ローカルは `.dev.vars`、本番はCloudflareのSecret
- **スキーマ変更は必ず `migrations/` にファイルで。** D1 Consoleでの手貼りは禁止（ローカルと本番がズレる）

## コンフリクトしやすい場所

大半のファイルは領域ごとに分かれているので衝突しません。注意が要るのはここだけです。

| 場所 | ルール |
|---|---|
| `migrations/` | **番号はIssueのコメントで宣言してから取る。** 迷ったら `0021_20260903_xxx.sql` のように日付を挟む |
| `public/locales/*.js` | 追加は各セクションの末尾に。既存キーの削除・並べ替えをしない |
| `public/tokens.css` | 触る人を1人に固定。必要なら依頼する |
| `src/review.ts` | ②違和感チェックと③修正が同居。この2つは同じ人が持つ |
| `CLAUDE.md` | **PRでは触らない。** 設計の決定はNotionの決定ログへ、実装の詳細はPRの説明文へ |

## 困ったら

- 詰まったら**30分で聞く。** 一人で溶かすより、聞いたほうがチーム全体の得です
- gitでコンフリクトしたら、自力で直そうとせず先に相談してOK。mainは保護されているので壊せません
- 「今週なにもできませんでした」は歓迎される報告です。黙って消えるほうが困ります
